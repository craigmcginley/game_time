<!DOCTYPE html>
<html lang="en">
  <head>
    <title>NFL Stats</title>
    <link href="styles.css" rel="stylesheet" type="text/css" />
  </head>

  <body>

  <h2>Leaderboard</h2>

    <ul>
      <% @team_stats.each do |team, stats| %>
        <li>
          <b><a href="/teams/<%= team %>"><%= team %></a></b>:
          <ul>
            <li>Wins:<%= stats[:wins] %></li>
            <li>Losses:<%= stats[:losses] %></li>
            <li>Ties:<%= stats[:ties] %></li>
          </ul>
        </li>
      <% end %>
    </ul>

  </body>

</html>
